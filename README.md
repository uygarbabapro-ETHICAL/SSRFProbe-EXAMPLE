# SSRFProbe-EXAMPLE
To use it, replace example.com with your desired URL.
SSRFProbe User Guide 1. Overview SSRFProbe is a simple Java tool that sends HTTP GET requests by checking a specific path. Its purposes are: Testing HTTP endpoints Checking secure path validation Displaying the response returned from the server in the terminal The program only sends requests for paths that contain /safepath. 2. Requirements To run the program: Java 8 or higher Terminal / command line To check the Java version: Copy the code java -version 3. Compilation To compile the Java file: Copy the code javac SsrfExample.java After this process: Copy the code The SsrfExample.class file is created. 4. Usage The basic function of the program: Copy the code getRequest(String dest) The dest parameter specifies the path to which the request will be sent. Example Usage Copy Code SsrfExample.getRequest("/safepath/test"); Actual URL to send: Copy Code https://example.com/safepath/test 5. Security Check The program checks: Copy Code if (!dest.contains("/safepath")) If /safepath does not exist in the path: Copy Code The message "path must be safe!" is displayed and the request is not sent. 6. Example Operation Valid request Copy Code /safepath/data Output: Copy Code <response from server> Invalid request Copy Code /admin Output: Copy Code path must be safe! 7. Error Handling If a connection or network error occurs, the program will display the following message: Copy code An error occurred: <error message> 8. Notes The program only supports GET requests. The URL is fixed and uses: Copy code https://example.com The path validation is a simple string check.
